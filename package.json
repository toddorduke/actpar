{
  "name": "actpar",
  "version": "1.0.0",
  "private": true,
  "engines": {
    "node": "22.x"
  },
  "workspaces": [
    "client",
    "mobile",
    "shared"
  ],
  "scripts": {
    "build": "cd client && npm install && npm run build && cp -r dist ../dist"
  }
}
