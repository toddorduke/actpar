{
  "name": "actpar",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "client",
    "mobile",
    "shared"
  ],
  "scripts": {
    "build": "cd client && npm install && npm run build && cp -r dist ../dist"
  }
}
